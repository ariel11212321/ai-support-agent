# AI Support Agent - Dependencies
# Only essential packages for maximum compatibility

# Core LangGraph for workflow orchestration (as requested)
langgraph>=0.5.4

# Rich for beautiful CLI interface (easy to explain)
rich>=13.0.0

# Optional: Performance monitoring (can be excluded if needed)
psutil>=5.9.0